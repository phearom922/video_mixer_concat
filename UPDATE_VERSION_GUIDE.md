# คู่มือการอัพเดทเวอร์ชั่นใหม่

เอกสารนี้อธิบายกระบวนการอัพเดทเวอร์ชั่นใหม่ของ FlowMix Desktop App

## 📋 ภาพรวม

ระบบอัพเดทเวอร์ชั่นทำงานแบบอัตโนมัติ โดย Desktop App จะตรวจสอบเวอร์ชั่นใหม่จาก License Server ทุก 12 ชั่วโมง และแจ้งเตือนผู้ใช้เมื่อมีเวอร์ชั่นใหม่

## 🔄 กระบวนการอัพเดทเวอร์ชั่น

### 1. การสร้าง Release ใหม่ (Admin Dashboard)

#### ขั้นตอนที่ 1: เข้าสู่ระบบ Admin Dashboard
1. เปิดเบราว์เซอร์ไปที่ `http://localhost:3000` (หรือ URL ของ Admin Dashboard)
2. Login ด้วยบัญชี Admin

#### ขั้นตอนที่ 2: ไปที่หน้า Releases
1. คลิกที่เมนู **"Releases"** ใน Sidebar
2. คุณจะเห็นรายการ Releases ทั้งหมดที่เคยสร้างไว้

#### ขั้นตอนที่ 3: สร้าง Release ใหม่
1. คลิกปุ่ม **"Create Release"**
2. กรอกข้อมูลในฟอร์ม:
   - **Platform**: เลือก `windows` (ตอนนี้รองรับแค่ Windows)
   - **Version**: ใส่เวอร์ชั่นใหม่ในรูปแบบ SemVer (เช่น `1.2.3`)
     - รูปแบบ: `MAJOR.MINOR.PATCH`
     - ตัวอย่าง: `1.0.0`, `1.1.0`, `2.0.0`
     - ⚠️ **สำคัญ**: ตรวจสอบให้แน่ใจว่าเวอร์ชั่นถูกต้อง เพราะ **Version ไม่สามารถแก้ไขได้หลังสร้าง** (มี UNIQUE constraint ใน database)
   - **Download URL**: URL สำหรับดาวน์โหลดไฟล์ติดตั้ง (เช่น `https://example.com/download/FlowMix-1.2.3.exe`)
     - ✅ สามารถแก้ไขได้หลังสร้าง (ดูส่วน "การแก้ไข Release" ด้านล่าง)
     - ควรทดสอบ URL ก่อนสร้าง Release
   - **Release Notes**: ข้อความอธิบายการเปลี่ยนแปลงในเวอร์ชั่นนี้
     - ตัวอย่าง:
       ```
       - เพิ่มฟีเจอร์ Dark Mode
       - แก้ไขบั๊กการแสดงผล
       - ปรับปรุงประสิทธิภาพ
       ```
     - ✅ สามารถแก้ไขได้หลังสร้าง (ดูส่วน "การแก้ไข Release" ด้านล่าง)
   - **Set as latest release**: ✅ ติ๊กถูกเพื่อตั้งเป็นเวอร์ชั่นล่าสุดสำหรับ platform นี้
3. คลิกปุ่ม **"Save"**

#### ขั้นตอนที่ 4: ตั้งค่าเป็น Latest Release (ถ้ายังไม่ได้ติ๊ก)
- ถ้า Release ที่สร้างยังไม่ได้ตั้งเป็น Latest:
  1. หา Release ที่ต้องการในตาราง
  2. คลิกปุ่ม **"Set Latest"**
  3. ยืนยันการตั้งค่า

### 2. การแก้ไข Release (Admin Dashboard)

#### ขั้นตอนที่ 1: ไปที่หน้า Releases
1. คลิกที่เมนู **"Releases"** ใน Sidebar
2. คุณจะเห็นรายการ Releases ทั้งหมด

#### ขั้นตอนที่ 2: แก้ไข Release
1. หา Release ที่ต้องการแก้ไขในตาราง
2. คลิกปุ่ม **"Edit"** ในคอลัมน์ Actions
3. ฟอร์มแก้ไขจะแสดงขึ้นมา
4. แก้ไขข้อมูลที่ต้องการ:
   - ✅ **Platform**: แก้ไขได้
   - ❌ **Version**: **ไม่สามารถแก้ไขได้** (แสดงเป็น disabled field)
   - ✅ **Download URL**: แก้ไขได้
   - ✅ **Release Notes**: แก้ไขได้
   - ✅ **Set as latest release**: แก้ไขได้
5. คลิกปุ่ม **"Update"**

> **หมายเหตุ**: 
> - **Version ไม่สามารถแก้ไขได้** เพราะมี UNIQUE constraint ใน database (`UNIQUE(platform, version)`)
> - ถ้าต้องการเปลี่ยน Version ต้องสร้าง Release ใหม่
> - การแก้ไข Platform, Download URL, Release Notes, หรือ is_latest จะไม่ส่งผลกระทบต่อ Version

> **หมายเหตุ**: เมื่อตั้ง Release ใหม่เป็น Latest, Release เก่าจะถูกยกเลิกการเป็น Latest อัตโนมัติ

### 3. การตรวจสอบอัพเดท (Desktop App)

#### การทำงานอัตโนมัติ
Desktop App จะตรวจสอบอัพเดทอัตโนมัติ:
- **ทุก 12 ชั่วโมง** หลังจากเปิดโปรแกรม
- เมื่อเปิดโปรแกรมครั้งแรก
- เมื่อเรียกใช้ `check_for_updates(force=True)`

#### กระบวนการตรวจสอบ
1. Desktop App ส่ง request ไปที่ License Server:
   ```
   GET /api/v1/releases/latest?platform=windows&current_version=1.0.0
   ```

2. License Server ตรวจสอบ:
   - หา Release ที่มี `is_latest = true` สำหรับ platform `windows`
   - เปรียบเทียบเวอร์ชั่นกับ `current_version` ที่ส่งมา
   - ถ้าเวอร์ชั่นใหม่กว่า จะส่งข้อมูลกลับมา

3. Desktop App รับข้อมูล:
   ```json
   {
     "update_available": true,
     "latest_version": "1.2.3",
     "release_notes": "- เพิ่มฟีเจอร์ Dark Mode\n- แก้ไขบั๊ก...",
     "download_url": "https://example.com/download/FlowMix-1.2.3.exe"
   }
   ```

4. แสดง Update Dialog:
   - แสดงเวอร์ชั่นใหม่
   - แสดง Release Notes
   - ปุ่ม **"Download"**: เปิด URL ดาวน์โหลดในเบราว์เซอร์
   - ปุ่ม **"Later"**: ปิด dialog (จะแจ้งอีกครั้งใน 12 ชั่วโมง)
   - ปุ่ม **"Skip This Version"**: ข้ามเวอร์ชั่นนี้ (จะไม่แจ้งอีก)

### 4. การดาวน์โหลดและติดตั้ง

1. ผู้ใช้คลิกปุ่ม **"Download"** ใน Update Dialog
2. เปิดเบราว์เซอร์ไปที่ Download URL
3. ดาวน์โหลดไฟล์ติดตั้ง
4. ติดตั้งเวอร์ชั่นใหม่ทับเวอร์ชั่นเก่า
5. เปิดโปรแกรมใหม่ - จะตรวจสอบอัพเดทอีกครั้ง

## 📝 ตัวอย่างการใช้งาน

### ตัวอย่างที่ 1: สร้าง Release ใหม่

**สถานการณ์**: คุณต้องการอัพเดทจากเวอร์ชั่น `1.0.0` เป็น `1.1.0`

1. **เตรียมไฟล์ติดตั้ง**:
   - Build โปรแกรมเป็น `FlowMix-1.1.0.exe`
   - อัพโหลดไปยัง hosting (เช่น GitHub Releases, Google Drive, หรือ server ของคุณ)
   - เก็บ URL เช่น `https://github.com/yourusername/FlowMix/releases/download/v1.1.0/FlowMix-1.1.0.exe`

2. **สร้าง Release ใน Admin Dashboard**:
   - Platform: `windows`
   - Version: `1.1.0`
   - Download URL: `https://github.com/yourusername/FlowMix/releases/download/v1.1.0/FlowMix-1.1.0.exe`
   - Release Notes:
     ```
     Version 1.1.0
     
     New Features:
     - เพิ่ม Dark Mode theme
     - เพิ่ม Sidebar ใน Admin Dashboard
     
     Bug Fixes:
     - แก้ไขปัญหา timezone ใน license expiration
     - แก้ไขปัญหา infinite loop ใน license detail page
     ```
   - ✅ Set as latest release

3. **ผู้ใช้จะได้รับแจ้งเตือน**:
   - เมื่อเปิดโปรแกรม (ถ้าเปิดครั้งแรกหลังจาก 12 ชั่วโมง)
   - หรือเมื่อครบ 12 ชั่วโมงหลังจากเปิดโปรแกรม

### ตัวอย่างที่ 2: อัพเดทหลายเวอร์ชั่น

**สถานการณ์**: คุณมี Release หลายเวอร์ชั่น แต่ต้องการให้ผู้ใช้อัพเดทไปที่เวอร์ชั่นล่าสุดเท่านั้น

- Release `1.0.0` (is_latest = false)
- Release `1.1.0` (is_latest = false)
- Release `1.2.0` (is_latest = true) ← ผู้ใช้จะเห็นเวอร์ชั่นนี้เท่านั้น

เมื่อสร้าง Release `1.3.0` และตั้งเป็น Latest:
- Release `1.2.0` จะถูกยกเลิกการเป็น Latest อัตโนมัติ
- Release `1.3.0` จะเป็น Latest ใหม่

## 🔧 การตั้งค่าและ Configuration

### Desktop App Configuration

ไฟล์: `desktop_app/app/services/update_service.py`

```python
CHECK_INTERVAL_HOURS = 12  # ตรวจสอบทุก 12 ชั่วโมง
```

### API Endpoints

#### Public Endpoint (ไม่ต้อง Authentication)
```
GET /api/v1/releases/latest?platform=windows&current_version=1.0.0
```

#### Admin Endpoints (ต้อง Authentication)
```
POST /api/v1/admin/releases
GET /api/v1/admin/releases?platform=windows
PUT /api/v1/admin/releases/{release_id}/set-latest
```

## ⚠️ ข้อควรระวัง

1. **Version Format**: ต้องใช้รูปแบบ SemVer (`MAJOR.MINOR.PATCH`) เท่านั้น
   - ✅ ถูกต้อง: `1.0.0`, `1.1.0`, `2.0.0`
   - ❌ ผิด: `1.0`, `v1.0.0`, `1.0.0-beta`

2. **Download URL**: ต้องเป็น URL ที่เข้าถึงได้จริงและสามารถดาวน์โหลดได้
   - ✅ **สามารถแก้ไขได้** หลังสร้าง Release (ดูส่วน "การแก้ไข Release")
   - ใช้ HTTPS เพื่อความปลอดภัย
   - ควรทดสอบ URL ก่อนสร้าง Release

3. **การแก้ไข Release**: ระบบรองรับการแก้ไข Release ที่สร้างแล้ว
   - ✅ **แก้ไขได้**: Platform, Download URL, Release Notes, is_latest
   - ❌ **แก้ไขไม่ได้**: Version (มี UNIQUE constraint ใน database)
   - ถ้าต้องการเปลี่ยน Version → ต้องสร้าง Release ใหม่ด้วยเวอร์ชั่นใหม่กว่า

4. **Latest Release**: มีได้เพียง 1 Release ต่อ Platform
   - เมื่อตั้ง Release ใหม่เป็น Latest, Release เก่าจะถูกยกเลิกอัตโนมัติ

5. **Version Comparison**: ระบบจะเปรียบเทียบเวอร์ชั่นแบบ SemVer
   - `1.2.3` > `1.2.2` ✅
   - `1.3.0` > `1.2.9` ✅
   - `2.0.0` > `1.9.9` ✅

6. **Skipped Versions**: ผู้ใช้สามารถข้ามเวอร์ชั่นได้
   - ถ้าผู้ใช้ข้ามเวอร์ชั่น `1.1.0`, จะไม่ได้รับแจ้งเตือนสำหรับเวอร์ชั่นนี้
   - แต่จะได้รับแจ้งเตือนสำหรับเวอร์ชั่นใหม่กว่า (เช่น `1.2.0`)

7. **Database Constraint**: มี constraint `UNIQUE(platform, version)`
   - ไม่สามารถมี Release ที่มี platform และ version เดียวกันได้
   - ถ้าต้องการแก้ไขข้อมูล ต้องสร้าง Release ใหม่ด้วยเวอร์ชั่นใหม่กว่า

## 🐛 Troubleshooting

### ปัญหา: ผู้ใช้ไม่ได้รับแจ้งเตือนอัพเดท

**สาเหตุที่เป็นไปได้**:
1. Release ยังไม่ได้ตั้งเป็น Latest
   - **แก้ไข**: ไปที่ Admin Dashboard → Releases → คลิก "Set Latest"

2. เวอร์ชั่นใหม่ไม่ใหม่กว่าเวอร์ชั่นปัจจุบัน
   - **แก้ไข**: ตรวจสอบว่า Version ที่ใส่ใหม่กว่าเวอร์ชั่นปัจจุบันจริงๆ

3. ยังไม่ครบ 12 ชั่วโมง
   - **แก้ไข**: รอให้ครบ 12 ชั่วโมง หรือบังคับตรวจสอบอัพเดท

4. ผู้ใช้ข้ามเวอร์ชั่นนี้ไปแล้ว
   - **แก้ไข**: สร้าง Release ใหม่ด้วยเวอร์ชั่นที่สูงกว่า

### ปัญหา: Download URL ไม่ทำงาน

**สาเหตุที่เป็นไปได้**:
1. URL ผิดพลาด
   - **วิธีแก้ไข**: 
     - ✅ **แก้ไข URL ได้**: ไปที่หน้า Releases → คลิก "Edit" → แก้ไข Download URL → คลิก "Update"
     - หรือสร้าง Release ใหม่ด้วยเวอร์ชั่นใหม่กว่า (ถ้าต้องการเปลี่ยน Version ด้วย)

2. ไฟล์ถูกลบหรือย้าย
   - **วิธีแก้ไข**: 
     - ✅ **แก้ไข URL ได้**: ไปที่หน้า Releases → คลิก "Edit" → แก้ไข Download URL เป็น URL ใหม่ → คลิก "Update"
     - หรืออัพโหลดไฟล์ใหม่และแก้ไข URL

3. Server ไม่สามารถเข้าถึงได้
   - **แก้ไข**: ตรวจสอบว่า hosting service ยังทำงานอยู่
   - ถ้า URL ต้องเปลี่ยน: แก้ไข URL ได้โดยตรง (ไม่ต้องสร้าง Release ใหม่)

> **หมายเหตุ**: ระบบรองรับการแก้ไข Release แล้ว (ยกเว้น Version) หาก URL หรือ Release Notes ผิดพลาด สามารถแก้ไขได้โดยตรงโดยไม่ต้องสร้าง Release ใหม่

## 📚 ข้อมูลเพิ่มเติม

- **SemVer Specification**: https://semver.org/
- **Admin Dashboard**: `http://localhost:3000/releases`
- **API Documentation**: ดูที่ `license_server/app/routers/admin.py` และ `license_server/app/routers/public.py`
